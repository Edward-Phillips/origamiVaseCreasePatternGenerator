import React from 'react';

export default function Origami() {
  return (
    <div>
      this is the Origami component, I'm probably going to store some pictures of my origami in an S3 bucket, set up a backend to manage that and do an API call to fill out cards with each one.
    </div>
  )
}
